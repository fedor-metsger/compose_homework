## Решение к домашнему заданию по лекции «Docker Compose»

### Описание установки и запуска

Перед запуском нужно выполнить команду `docker-compose build`, она создаст образ с Django.

Затем можно запускать с помощью команды `docker-compose up`
